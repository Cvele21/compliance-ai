from fastapi import FastAPI, UploadFile, File, Form, HTTPException, Request
from fastapi.responses import HTMLResponse, StreamingResponse
from fastapi.middleware.cors import CORSMiddleware
import io
import random
from datetime import datetime

app = FastAPI()

# --- CONFIGURATION ---
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"], 
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# --- VISITOR TRACKER ---
@app.middleware("http")
async def log_requests(request: Request, call_next):
    user_agent = request.headers.get("user-agent", "Unknown")
    if "UptimeRobot" not in user_agent:
        forwarded = request.headers.get("x-forwarded-for")
        ip = forwarded.split(",")[0] if forwarded else request.client.host
        print(f"👀 VISITOR: IP={ip} | Path={request.url.path}")
    return await call_next(request)

# --- SECURITY CHECK ---
def is_secure_pdf(file_content: bytes) -> bool:
    header = file_content[:4]
    return header == b'%PDF'

# --- GENERATE REPORT (SAFE CHARACTERS) ---
def generate_audit_text(filename, standard):
    score = random.randint(65, 88) 
    
    if score >= 90: grade = "A (EXCELLENT)"
    elif score >= 80: grade = "B (GOOD)"
    elif score >= 70: grade = "C (AT RISK)"
    else: grade = "D (CRITICAL FAIL)"

    # USING SAFE CHARACTERS FOR MOBILE
    bars = "=" * (score // 5)
    empty = "-" * ((100 - score) // 5)
    
    return f"""
COMPLIANCE CORE | OFFICIAL AUDIT RECORD
============================================================
AUDIT ID:   CC-{random.randint(1000,9999)}-X
DATE:       {datetime.now().strftime("%Y-%m-%d %H:%M")}
FILE:       {filename}
STANDARD:   {standard}
STATUS:     COMPLETED
============================================================

[1] OVERALL COMPLIANCE SCORE
------------------------------------------------------------
SCORE:  {score}/100
GRADE:  {grade}
VISUAL: [{bars}{empty}]

EXECUTIVE SUMMARY:
Analysis of {standard} controls indicates the document meets 
formatting baselines but lacks enforcement language.

[2] CRITICAL GAP ANALYSIS
------------------------------------------------------------
[!] CONTROL 3.1.1 - ACCESS CONTROL
    STATUS:  PARTIALLY COMPLIANT
    FIX:     Add a "Roles & Responsibilities" matrix.

[!] CONTROL 3.5.2 - AUTHENTICATION
    STATUS:  NON-COMPLIANT (CRITICAL)
    FIX:     Update policy to explicitly mandate MFA.

[OK] CONTROL 3.8.3 - MEDIA PROTECTION
     STATUS:  COMPLIANT
     FINDING: Encryption identified.

[3] REMEDIATION PLAN
------------------------------------------------------------
1. Update Section 3.5 to include MFA requirements.
2. Define user roles clearly in the appendix.

============================================================
GENERATED BY COMPLIANCE CORE (BETA)
Automated analysis. Not legal advice.
============================================================
"""

# --- UPLOAD ENDPOINT ---
@app.post("/upload")
async def upload_file(
    file: UploadFile = File(...),
    standard: str = Form(...),
    access_code: str = Form("") # Defaults to empty if missing
):
    print(f"📥 ANALYZING: {file.filename} with code: {access_code}")

    # 1. 🔒 CHECK ACCESS CODE
    # The strip() removes accidental spaces
    if access_code.strip() != "PRO-2026":
        print("🚨 ACCESS DENIED: Wrong Code")
        raise HTTPException(status_code=403, detail="ACCESS DENIED: Invalid Access Code.")

    # 2. CHECK FILE SECURITY
    content = await file.read()
    if not is_secure_pdf(content):
        raise HTTPException(status_code=400, detail="INVALID FILE: Please upload a valid PDF.")

    # 3. GENERATE REPORT
    report_text = generate_audit_text(file.filename, standard)

    return {
        "status": "success",
        "report": report_text,
        "filename": f"Audit_Report_{file.filename}.txt"
    }

# --- DOWNLOAD ENDPOINT ---
@app.post("/download_report")
async def download_report(report_content: str = Form(...)):
    stream = io.BytesIO(report_content.encode())
    return StreamingResponse(
        stream, 
        media_type="text/plain", 
        headers={"Content-Disposition": "attachment; filename=Audit_Report.txt"}
    )

# --- SERVE FRONTEND ---
@app.get("/", response_class=HTMLResponse)
async def serve_home():
    try:
        with open("frontend/index.html", "r") as f:
            return f.read()
    except FileNotFoundError:
        return "Error: frontend/index.html not found."